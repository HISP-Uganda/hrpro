# Prompt Archive Policy

Store milestone prompt history files locally in this directory using:

`YYYY-MM-DD_HHMM_<milestone_slug>.md`

Only this `README.md` and `.gitkeep` should be committed.
Prompt history files are local-only and ignored by Git.
